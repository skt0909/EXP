# Copy to /etc/systemd/journald.conf.d/fpl.conf, then:
#   sudo systemctl restart systemd-journald
# The worker logs every minute on match days; without a cap journald can
# fill the e2-micro's 10 GB disk over a season.
[Journal]
SystemMaxUse=200M
